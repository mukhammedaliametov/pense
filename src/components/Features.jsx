import React from 'react';
import FeaturesImage

const Features = () => {
    return (
        <div style={{backgroundImage: `url(${})`}}>
            
        </div>
    );
};

export default Features;